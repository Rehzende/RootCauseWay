apiVersion: v1
kind: Service
metadata:
  name: {{ include "rootcauseway.fullname" . }}-orchestrator
  labels:
    {{- include "rootcauseway.labels" . | nindent 4 }}
    app.kubernetes.io/component: orchestrator
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.orchestrator.port }}
      targetPort: {{ .Values.orchestrator.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "rootcauseway.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: orchestrator
